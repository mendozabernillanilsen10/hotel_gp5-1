server:
 servlet:
  context-path: /sistema_hotel
spring:
 datasource:  
  url: jdbc:mysql://localhost/BD_Emely_Hotel?useSSL=false&serverTimezone = GMT
  username: root
  password: 
  hikari:
   connectionTimeout: 30000
   idleTimeout: 60000
   maxLifetime: 60000
   maximumPoolSize: 15
 jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect = org.hibernate.dialect.MySQL5Dialect
logging:
  level:
    root: WARN    
Jwt: 
  password: miclave12345678987654321    
